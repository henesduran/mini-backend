# Mini Backend

This is a simple Node.js/Express server with two JSON endpoints.

## Setup
1. Run `npm install` to install dependencies.
2. Run `node index.js` to start the server.

## Endpoints
- `GET /` - Welcome message
- `GET /status` - Server status information
